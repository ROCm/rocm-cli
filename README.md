# Progress

WIP progress files for cross-machine context preservation.

This branch is an orphan — it shares no history with main. It holds PERSONAL WIP
notes and MUST NEVER be pushed to the ROCm/rocm-cli origin. Stored locally only
(push guard: branch.progress.pushRemote is set to a disabled remote).
Managed by the wip-management skill.
